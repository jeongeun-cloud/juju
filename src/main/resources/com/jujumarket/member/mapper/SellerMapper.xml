<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jujumarket.member.mapper.SellerMapper">

	<insert id="insert">
		insert into m_shop(idNo, shopName, shopAddr, contact1)
		values(#{idNo}, #{shopName}, #{shopAddr}, #{contact1})

	</insert>


	<update id="update">
		update m_shop
		set shopAddr = #{shopAddr},
			contact1 = #{contact1},
			contact2 = #{contact2},
			postCode = #{shopPostCode}
	</update>
	



</mapper>