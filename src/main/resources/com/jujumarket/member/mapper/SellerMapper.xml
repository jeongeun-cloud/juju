<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jujumarket.member.mapper.SellerMapper">

	<insert id="insert">
		insert into m_shop(idNo, shopName, pwd, shopAddr,
		contact1)
		values(#{idNo}, #{shopName}, #{pwd}, #{shopAddr},
		#{contact1})

	</insert>



	<select id="getSellerPwdByIdNo" resultType="String">
		select pwd
		from m_shop
		where idNo = #{idNo}


	</select>



</mapper>