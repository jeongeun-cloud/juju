<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jujumarket.member.mapper.CustomerMapper">

	<insert id="insert">
		insert into m_customer(idNo, pwd, contact)
		values(#{idNo}, #{pwd}, #{contact})

	</insert>

	<select id="getCustomerPwdByIdNo" resultType="String">
		select pwd
		from m_customer
		where idNo = #{idNo}


	</select>



</mapper>