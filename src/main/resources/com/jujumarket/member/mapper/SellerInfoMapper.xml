<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jujumarket.member.mapper.SellerInfoMapper">

	<!-- 파일업로드 후 이미지 추가해주기 -->
	<!-- 권한코드는 삭제되었음 2020-07-15 -->
	<insert id="insertShopInfo">
		insert into m_shop_info(idNo, businessCode, bank, bankAccount)
		values(#{idNo}, #{businessCode}, #{bank}, #{bankAccount})

	</insert>

	<update id="update">
		update m_shop_info
		set bank = #{bank}
			bankAccount = #{bankAccount}
			thumbNail = #{thumbNail}
			
	</update>






</mapper>